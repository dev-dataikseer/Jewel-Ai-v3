[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.worker"

# Celery worker + beat. Leave Railway Root Directory empty (repo root).
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
