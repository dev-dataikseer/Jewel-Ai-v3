[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.worker"

# Celery worker only (Beat is a separate service — see railway.beat.toml).
# Leave Railway Root Directory empty (repo root).
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
