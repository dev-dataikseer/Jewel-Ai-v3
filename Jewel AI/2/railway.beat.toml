[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.beat"

# Celery Beat only — keep replicas=1. Leave Railway Root Directory empty (repo root).
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
