[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/docker/Dockerfile.worker"

# Celery workers only — Beat is a separate service (see railway.beat.toml / Dockerfile.beat).
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
