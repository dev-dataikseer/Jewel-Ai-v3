[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/docker/Dockerfile.beat"

# Celery Beat only — keep replicas=1. Root Directory = Jewel AI when using this nested config.
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
