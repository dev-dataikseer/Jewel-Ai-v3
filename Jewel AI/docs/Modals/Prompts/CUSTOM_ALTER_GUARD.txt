# Backend pre-check reference list — NOT injected into the model prompt.
# Scan {{USER_CUSTOM_INSTRUCTION}} for phrases matching these patterns before assembly.
# On a match, strip the matched clause or reroute the job to GEMSTONE_COLOR_CHANGE
# with explicit-exception phrasing instead of letting free text silently fight the lock.

resize the (ring|stone|diamond|gem|band)
make the (stone|diamond|gem) (bigger|smaller|larger)
change the (metal|gold|silver|band) color
recolor the (stone|gem|diamond)
add (more|extra) (stones|diamonds|gems)
remove (a|the) (stone|diamond|prong|setting)
redesign the (setting|band|ring|piece)
change the (cut|shape) of the (stone|diamond|gem)
make it (look like|resemble) a different (design|style|piece)