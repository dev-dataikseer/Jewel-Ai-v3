[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Repo root configs — app sources live under Jewel AI/.
# Do NOT set Railway Root Directory (leave empty / "/").
[deploy]
healthcheckPath = "/ready"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
